services:
  - type: web
    name: ai-prompt-scheduler
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ENCRYPTION_SECRET
        generateValue: true
      - key: PORT
        value: 3000
    disk:
      name: scheduler-data
      mountPath: /app/data
      sizeGB: 1
